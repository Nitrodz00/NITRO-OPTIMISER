<UserControl x:Class="NitroOptimizer.Views.GamingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextWhiteBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="GAMING OPTIMIZATION" FontSize="24" FontWeight="Bold" Foreground="{StaticResource MagentaBrush}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <Button Content="Apply Registry Gaming Optimizations" Style="{StaticResource NeonButtonStyle}" Command="{Binding ApplyGamingTweaksCommand}" Height="45" Margin="0,5"/>
            <TextBlock Text="Disables GameDVR and enables Hardware Accelerated GPU Scheduling (Requires Restart)." Foreground="Gray" FontSize="12" Margin="5,0,0,10" TextWrapping="Wrap"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
