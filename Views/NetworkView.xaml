<UserControl x:Class="NitroOptimizer.Views.NetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextWhiteBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="NETWORK TWEAKS" FontSize="24" FontWeight="Bold" Foreground="{StaticResource CyanBrush}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Repairs -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Repairs" Foreground="Gray" FontWeight="Bold" Margin="5,0,0,5"/>
                <Button Content="Flush DNS" Style="{StaticResource NeonButtonStyle}" Command="{Binding FlushDnsCommand}" Height="40" Margin="0,5"/>
                <Button Content="Reset Winsock" Style="{StaticResource NeonButtonStyle}" Command="{Binding ResetWinsockCommand}" Height="40" Margin="0,5"/>
                <Button Content="Reset TCP/IP" Style="{StaticResource NeonButtonStyle}" Command="{Binding ResetTcpIpCommand}" Height="40" Margin="0,5"/>
            </StackPanel>

            <!-- DNS -->
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="DNS Optimization" Foreground="Gray" FontWeight="Bold" Margin="5,0,0,5"/>
                <Button Content="Apply Google DNS (8.8.8.8)" Style="{StaticResource NeonButtonStyle}" Command="{Binding SetGoogleDnsCommand}" Height="40" Margin="0,5"/>
                <Button Content="Apply Cloudflare DNS (1.1.1.1)" Style="{StaticResource NeonButtonStyle}" Command="{Binding SetCloudflareDnsCommand}" Height="40" Margin="0,5"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="{StaticResource MagentaBrush}" FontWeight="Bold" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
