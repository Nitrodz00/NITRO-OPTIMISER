<UserControl x:Class="NitroOptimizer.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextWhiteBrush}">

    <UserControl.Resources>
        <Style TargetType="ProgressBar" x:Key="NeonProgress">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Foreground">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="{StaticResource MagentaAccent}" Offset="0.0"/>
                        <GradientStop Color="{StaticResource CyanAccent}" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#222233"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SYSTEM DASHBOARD" FontSize="24" FontWeight="Bold" Foreground="{StaticResource CyanBrush}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <!-- System Health & Live Temperature Monitor -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#11111A" CornerRadius="8" Padding="15" Margin="0,0,5,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Health:" FontSize="16" Foreground="Gray" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SystemStatus}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource MagentaBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#11111A" CornerRadius="8" Padding="15" Margin="5,0,5,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="CPU Temp:" FontSize="16" Foreground="Gray" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding CpuTemp}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource CyanBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="#11111A" CornerRadius="8" Padding="15" Margin="5,0,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="GPU Temp:" FontSize="16" Foreground="Gray" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding GpuTemp}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource CyanBrush}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Drives Section -->
            <TextBlock Text="Storage Drives" FontSize="18" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <ItemsControl ItemsSource="{Binding Drives}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#11111A" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <Grid>
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding UsageText}" FontSize="14" Foreground="Gray" HorizontalAlignment="Right"/>
                                </Grid>
                                
                                <ProgressBar Grid.Row="1" Minimum="0" Maximum="100" Value="{Binding UsagePercentage}" Style="{StaticResource NeonProgress}" Margin="0,10,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Quick Dashboard Actions -->
            <TextBlock Text="Quick Actions" FontSize="18" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" Margin="0,15,0,10"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Restore Windows Defaults" Style="{StaticResource NeonButtonStyle}" Command="{Binding RevertTweaksCommand}" Height="40" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="Toggle Floating Stats Widget" Style="{StaticResource NeonButtonStyle}" Command="{Binding ToggleWidgetCommand}" Height="40" Margin="5,0,0,0"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
