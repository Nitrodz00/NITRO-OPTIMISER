<UserControl x:Class="NitroOptimizer.Views.CleaningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextWhiteBrush}">

    <UserControl.Resources>
        <Style TargetType="ProgressBar" x:Key="NeonProgress">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Foreground">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="{StaticResource MagentaAccent}" Offset="0.0"/>
                        <GradientStop Color="{StaticResource CyanAccent}" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#222233"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="SYSTEM CLEANER" FontSize="24" FontWeight="Bold" Foreground="{StaticResource CyanBrush}" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <Button Content="Clean Temp Files" Style="{StaticResource NeonButtonStyle}" Command="{Binding CleanTempCommand}" Height="40" Margin="0,5"/>
                <Button Content="Clean DirectX Shader Cache" Style="{StaticResource NeonButtonStyle}" Command="{Binding CleanDirectXCommand}" Height="40" Margin="0,5"/>
                <Button Content="Clean NVIDIA &amp; AMD Cache" Style="{StaticResource NeonButtonStyle}" Command="{Binding CleanGpuCommand}" Height="40" Margin="0,5"/>
                <Button Content="Clean Windows Update Cache" Style="{StaticResource NeonButtonStyle}" Command="{Binding CleanWindowsUpdateCommand}" Height="40" Margin="0,5"/>
                <Button Content="Clean FiveM/CitizenFX Cache" Style="{StaticResource NeonButtonStyle}" Command="{Binding CleanFiveMCommand}" Height="40" Margin="0,5"/>
                <Button Content="Clean RAM (Standby Cache)" Style="{StaticResource NeonButtonStyle}" Command="{Binding CleanRamCommand}" Height="40" Margin="0,5"/>
                
                <Button Content="CLEAN ALL" Background="{StaticResource MagentaBrush}" Foreground="White" BorderThickness="0" FontWeight="Bold" Height="45" Margin="0,20,0,0" Command="{Binding CleanAllCommand}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <Border Background="#11111A" CornerRadius="8" Padding="30" Margin="20,0,0,0" Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Total Freed Space:" Foreground="Gray" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <Viewbox Grid.Row="1" Stretch="Uniform" StretchDirection="DownOnly" Margin="0,0,0,20">
                        <TextBlock Text="{Binding TotalFreed}" Foreground="{StaticResource CyanBrush}" FontSize="48" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </Viewbox>

                    <ProgressBar Grid.Row="2" Minimum="0" Maximum="100" Value="{Binding ProgressValue}" Height="10" Style="{StaticResource NeonProgress}" Margin="0,0,0,10"/>
                    <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" HorizontalAlignment="Center" Foreground="LightGray" TextWrapping="Wrap" TextAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
