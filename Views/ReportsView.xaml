<UserControl x:Class="NitroOptimizer.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextWhiteBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="SYSTEM REPORTS" FontSize="24" FontWeight="Bold" Foreground="{StaticResource MagentaBrush}" Margin="0,0,0,10"/>

        <!-- Display Area -->
        <Border Grid.Row="1" Background="#11111A" CornerRadius="8" Margin="0,10,0,10">
            <TextBox Text="{Binding ReportText}" 
                     Foreground="LightGray" 
                     Background="Transparent"
                     BorderThickness="0"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Margin="15"
                     FontFamily="Consolas"
                     FontSize="13"/>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
            <Button Content="Generate Report" Style="{StaticResource NeonButtonStyle}" Command="{Binding GenerateReportCommand}" Width="150" Margin="0,0,10,0"/>
            <Button Content="Save to Desktop" Style="{StaticResource NeonButtonStyle}" Command="{Binding SaveReportCommand}" BorderBrush="{StaticResource CyanBrush}" Width="150"/>
            <TextBlock Text="{Binding StatusMessage}" Foreground="Gray" VerticalAlignment="Center" Margin="15,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
