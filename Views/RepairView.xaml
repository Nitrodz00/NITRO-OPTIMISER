<UserControl x:Class="NitroOptimizer.Views.RepairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{StaticResource TextWhiteBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="SYSTEM REPAIR" FontSize="24" FontWeight="Bold" Foreground="{StaticResource MagentaBrush}" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <Button Content="Run SFC /Scannow" Style="{StaticResource NeonButtonStyle}" Command="{Binding RunSfcScanCommand}" Height="40" Margin="0,5"/>
            <TextBlock Text="Scans and repairs corrupted Windows system files." Foreground="Gray" FontSize="12" Margin="5,0,0,10"/>
            
            <Button Content="Run DISM RestoreHealth" Style="{StaticResource NeonButtonStyle}" Command="{Binding RunDismCommand}" Height="40" Margin="0,5"/>
            <TextBlock Text="Repairs the Windows image using Windows Update." Foreground="Gray" FontSize="12" Margin="5,0,0,10"/>

            <Button Content="Rebuild Icon Cache" Style="{StaticResource NeonButtonStyle}" Command="{Binding RebuildIconCacheCommand}" Height="40" Margin="0,5"/>
            <TextBlock Text="Fixes blank or corrupted icons (Restarts Explorer)." Foreground="Gray" FontSize="12" Margin="5,0,0,10"/>

            <Border Background="#11111A" CornerRadius="8" Padding="15" Margin="0,15,0,0">
                <StackPanel>
                    <TextBlock Text="Context Menu Manager" FontSize="16" FontWeight="Bold" Foreground="{StaticResource CyanBrush}" Margin="0,0,0,10"/>
                    <CheckBox Content="Disable Junk Desktop Context Menus (NVIDIA, etc. to fix right-click delay)" 
                              IsChecked="{Binding DisableJunkContextMenu}" 
                              Foreground="White" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="{StaticResource CyanBrush}" FontWeight="Bold" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
